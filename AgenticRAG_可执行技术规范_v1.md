# Agentic-RAG 动态长期寻物：可执行技术规范 v1

> 日期：2026-03-21  
> 目标：把你的论文主线固化为可直接实现与验证的统一技术基线。  
> 当前范围：先聚焦“建图 + 融合 + 查询 + 概率地图”，暂不包含真实控制闭环。

---

## 1. 问题定义与输出形式

### 1.1 任务定义
给定文本查询 $q$（如 `find a chair`），在动态环境中输出目标物体可能存在的**空间概率分布**，而非单一绝对坐标。

### 1.2 最终输出
- 概率图 $P(x, y \mid q, t)$（2D 可通行平面）
- Top-K 候选探索点 $\{p_A, p_B, p_C\}$
- 每个候选点的置信度与来源分解（RAG 分数、Agent 分数）

---

## 2. 统一数据模型（你后续代码都围绕这个）

### 2.1 时序分层语义图
每次探索得到一张分层场景图 $G_t = (V_t, E_t)$：
- 楼层层（Floor）
- 房间层（Room）
- 物体层（Object）

### 2.2 物体节点字段（Object Node）
每个物体节点 $o_i$ 至少包含：
- `obj_id`：全局稳定 ID（不能只在房间局部唯一）
- `category`：物体语义标签
- `clip_embedding`：视觉特征向量（来自真实视觉提取）
- `pos_3d` / `pos_2d`：位置
- `bbox_3d`：3D 边界框
- `last_update_time`：最近更新时间戳
- `stability`：动态稳定性先验，范围 $[0,1]$
- `cooccur_stats`：与邻近物体共现计数（用于 $R_{cfd}$）
- `exist_prob`：该节点当前存在置信（可选但强烈建议）

---

## 3. 图融合规范（长期记忆核心）

## 3.1 匹配策略（T-1 与 T 融合）
对新图 $G_t$ 中节点，与历史图 $M_{t-1}$ 做匹配：
1. 语义候选过滤：同类目或语义近邻类目
2. 空间重叠评分：3D IoU + 中心点距离
3. 外观一致性：视觉向量余弦相似度
4. 联合匹配：
$$
S_{match}(i,j)=\eta_1\,IoU_{3D}(i,j)+\eta_2\,\cos(f_i,f_j)-\eta_3\,d(i,j)
$$
若 $S_{match}>\tau_{match}$ 视为同一实体，否则新建节点。

**参数解释**：
- $i, j$：历史图中的节点 $i$ 和新图中的节点 $j$
- $IoU_{3D}(i,j)$：3D 边界框交并比，范围 $[0,1]$，值越大表示物体位置越相似
- $\cos(f_i, f_j)$：CLIP 特征向量的余弦相似度，范围 $[-1,1]$（通常 $[0,1]$），值越大表示外观越相似
- $d(i,j)$：两节点中心的欧氏距离（米）
- $\eta_1, \eta_2, \eta_3$：**融合系数**（权重），需满足 $\eta_1 + \eta_2 + \eta_3 \approx 1$（归一化）
  - **建议值**：$\eta_1=0.4$（空间），$\eta_2=0.4$（外观），$\eta_3=0.2$（距离惩罚）
  - 调整策略：如果误匹配较多，增大 $\eta_2$；如果遗漏较多，减小 $\eta_3$
- $\tau_{match}$：**匹配阈值**，范围 $(0,1)$，**建议值** $0.55$
  - $\tau_{match}$ 越高 → 匹配更严格 → 易产生重复节点
  - $\tau_{match}$ 越低 → 匹配更宽松 → 易误合并不同物体

### 3.2 时间衰减（必须用真实时间差）
对节点使用真实时间差 $\Delta T$ 衰减：
$$
w_t(i)=\exp(-\lambda \Delta T_i)
$$
说明：该项替代“仅按扫描次数衰减”的旧方案。
**参数解释**：
- $w_t(i)$：第 $i$ 个节点的时间权重，范围 $[0,1]$
- $\Delta T_i = t_{now} - t_{last}$：当前时刻与最后一次观测该节点的时间差（秒或天等单位）
- $\lambda$：**时间衰减系数**，需结合 $\Delta T$ 的单位选择
  - 推荐方法：设定**半衰期** $T_{half}$（物体信息大致失效的时间），则 $\lambda = \frac{\ln 2}{T_{half}}$
  - 示例：若 $T_{half} = 3$ 天，且 $\Delta T$ 单位为秒，则 $\lambda = \frac{\ln 2}{3 \times 86400}$
  - 若 $T_{half} = 3$ 天，且 $\Delta T$ 单位为天，则 $\lambda = \frac{\ln 2}{3} \approx 0.231$
  - **直观理解**：$\Delta T = T_{half}$ 时，$w_t = e^{-\ln 2} = 0.5$（权重衰减到一半）
- **建议参数**：$T_{half} = 3$ 天（可根据环境动态性调整，静态环境更大，高动态环境更小）
### 3.3 节点状态更新
匹配成功时：
- 更新位置、视觉特征（指数滑动）
- 更新时间戳
- 累积共现统计
- 更新存在概率 `exist_prob`

未观测到时：
- 不立即删除，先降低 `exist_prob`
- 连续缺失超过阈值再标记为 `inactive`

---

## 4. 查询评分模型（RAG + Agent 双通道）

设查询文本向量为 $f_q$，候选节点为 $o_i$。

### 4.1 RAG 通道评分
1) 语义相似：
$$
R_{sim}(i)=\cos(f_q, f_i)
$$

2) 时间可信：
$$
R_{time}(i)=\exp(-\lambda \Delta T_i)
$$

3) 稳定性：
$$
R_{stab}(i)=stability_i
$$

> 说明：`R_{time}` 与 `R_{stab}` 不直接与其他项线性相加，而是共同构成“时间-稳定性指数门控因子”。

4) 时间-稳定性指数门控（核心改动）：
$$
G_{ts}(i)=\exp\!\left(-\frac{\lambda\,\Delta T_i}{1+\kappa\,R_{stab}(i)}\right)
$$
其中 $\kappa>0$ 为稳定性抗衰减系数。含义是：
- 时间越久（$\Delta T_i$ 越大），$G_{ts}(i)$ 越小，整体分数越降。
- 稳定性越高（$R_{stab}(i)$ 越大），分母越大，衰减越慢，即“更抗时间因子”。
**详细参数解释**：
- $G_{ts}(i)$：时间-稳定性复合门控因子，范围 $[0,1]$，用乘法应用到评分上
- $\lambda$：时间衰减系数（见第 3.2 节），单位应与 $\Delta T_i$ 一致
- $\Delta T_i$：最后观测时间与当前时间的差值
- $R_{stab}(i) = stability_i$：物体的稳定性属性，范围 $[0,1]$
- $\kappa$：**稳定性抗衰减系数**，控制稳定性对时间衰减的保护强度
  - $\kappa = 0$：完全忽略稳定性，所有物体衰减速度相同
  - $\kappa = 1.0$：**建议值**，稳定物体获得中等保护
  - $\kappa = 2.0$：稳定物体获得强保护（长期有效）
  - **调整策略**：如果实验中高稳定物体（如冰箱）的权重持续有效，增大 $\kappa$；如果权重衰减过快，也增大 $\kappa$

**示例计算**：
- 情景：杯子 ($R_{stab}=0.2$) vs 冰箱 ($R_{stab}=0.9$)，$\lambda \Delta T = 0.5$
- 杯子：$G_{ts} = \exp(-0.5 / (1 + 1.0 \times 0.2)) = \exp(-0.5/1.2) \approx 0.64$
- 冰箱：$G_{ts} = \exp(-0.5 / (1 + 1.0 \times 0.9)) = \exp(-0.5/1.9) \approx 0.77$
- 结论：在相同时间后，冰箱的信任度衰减更慢
5) 关联置信（related confidence）：
$$
R_{cfd}(q,i)=\frac{\sum_{k\in \mathcal{N}(i)} \mathbf{1}(k\in \mathcal{A}_q)\cdot count(i,k)}{\sum_{k\in \mathcal{N}(i)} count(i,k)+\epsilon}
$$
其中 $\mathcal{A}_q$ 是与查询物体相关的锚物体集合（可由统计或 Agent 给出）。

详细解释：
- $\mathcal{N}(i)$：节点 $o_i$ 的邻域物体集合（可由空间半径或拓扑边定义）。
- $count(i,k)$：在历史时序中，$o_i$ 与邻居 $k$ 的共现次数（或加权共现次数）。
- $\mathbf{1}(k\in\mathcal{A}_q)$：指示函数，若邻居 $k$ 属于查询 $q$ 的“语义锚集合”则为 1，否则为 0。
- 分子：只累计“与查询相关锚物体”的共现强度，表示“这个位置是否常和目标相关线索一起出现”。
- 分母：累计全部邻域共现强度，用于归一化，避免仅凭场景中邻居数量多就虚高。
- 因此 $R_{cfd}\in[0,1]$（近似），值越高表示该节点周边上下文越支持目标出现。

为什么要 $+\epsilon$：
- 当节点几乎没有邻域共现记录时，可能出现分母为 0（或极小），导致数值发散或不稳定。
- 加入一个很小常数 $\epsilon$（如 $10^{-8}$）可防止除零，并在冷启动稀疏阶段提供数值稳定性。
- 工程上建议：同时设置 `min_cooccur_count` 门槛；低于门槛时降低 $R_{cfd}$ 置信或回退到先验。

6) RAG 总分（先语义/关联融合，再乘指数门控）：
$$
S_{base}(i)=\beta_1\hat R_{sim}(i)+\beta_2\hat R_{cfd}(i),\quad \beta_1+\beta_2=1
$$
$$
S_{rag}(i)=S_{base}(i)\cdot G_{ts}(i)
$$
这保证了：时间与稳定性通过指数门控影响“整体可信度”，而非和语义项、关联项简单并列相加。

### 4.2 Agent 通道评分
Agent 输入：
- 查询词 $q$
- 当前 RAG 候选摘要（房间分布、共现物体、近期变化）
- 规则提示词（房间先验 + 动态常识）

Agent 输出：
- 房间级先验 $P_{agent}(room\mid q)$
- 节点/区域级补充分 $S_{agent}(i)$

### 4.3 动态融合权重（探索越多，越信 RAG）
令 $n$ 为当前任务累计探索轮次：
$$
w_{agent}(n)=\max(w_{min},\exp(-k n))
$$
$$
w_{rag}(n)=1-w_{agent}(n)
$$
最终分数：
$$
S(i)=w_{rag}(n)\cdot S_{rag}(i)+w_{agent}(n)\cdot S_{agent}(i)
$$

**参数解释**：
- $n$：当前任务累计的探索轮次（从 0 开始），每导航到一个候选点并扫描即 $n$ 加 1
- $w_{agent}(n)$：Agent 权重，从 1（初始）逐步衰减到 $w_{min}$（稳定值）
- $w_{rag}(n)$：RAG 权重，从 0（初始）逐步增长到 $(1-w_{min})$（稳定值）
- $k$：**衰减斜率**，控制权重转移的速度
  - $k$ 越小 → 转移越缓慢 → Agent 影响力长期存在
  - $k$ 越大 → 转移越快速 → 快速信任 RAG
  - **建议值**：$k=0.35$（探索 3-5 次后权重开始显著转移）
- $w_{min}$：**最小 Agent 权重**，即使探索次数很多，Agent 也保留最少影响力
  - **建议值**：$w_{min}=0.15$（15% 的权重始终留给先验知识，防止完全依赖可能有误的历史记忆）
- $S_{rag}(i)$：RAG 通道评分（第 4.1 节的 $S_{rag}(i)$）
- $S_{agent}(i)$：Agent 通道评分（第 4.2 节的 $S_{agent}(i)$）

**物理意义**：
- 初期（$n=0$ 时）：$w_{agent} \approx 1.0$，完全依赖大模型常识（因为没有足够探索数据）
- 中期（$n=3$ 时）：$w_{agent} \approx 0.35 + 0.15 = 0.50$，两种信息平衡融合
- 后期（$n>>k$ 时）：$w_{agent} \to 0.15$，主要依赖历史记忆，但保留常识修正

---

## 4.4 负向反馈与失败经验（Negative GMM）

### 核心理念："机器人记仇"
这是系统最关键的创新之一。当机器人在某位置搜索失败（即在 RAG 高置信区域视锥内未检测到目标），系统需要：
1. 不仅"停止加权"，而是**主动降权**该区域
2. 触发 Agent 的重新评估，生成备选假设
3. 为下一轮搜索提供排斥性指导（"已验证不在这里"）

### 4.4.1 假阴性观测的贝叶斯惩罚

定义"假阴性"事件：机器人当前视锥 $V_t$ 完全覆盖了某节点 $o_i$ 的空间范围，但视觉感知未检测到该物体。

在这种情况下，该节点的 `exist_prob` 应按贝叶斯更新急剧下降：
$$
exist\_prob_i^{new} = exist\_prob_i^{old} \times \gamma_{neg},\quad \gamma_{neg} \in [0.1, 0.3]
$$

其中 $\gamma_{neg}$ 为"验证失败衰减系数"。建议：
- 高稳定物体（如冰箱）：$\gamma_{neg}=0.15$（强降权）
- 中等物体（如椅子）：$\gamma_{neg}=0.25$
- 低稳定物体（如手机）：$\gamma_{neg}=0.35$（弱降权，因为容易被挡住或移走）

**参数解释**：
- $exist\_prob_i^{old}$：节点在触发假阴性前的存在概率
- $exists\_prob_i^{new}$：更新后的存在概率
- $\gamma_{neg}$：**负向衰减系数**，范围 $[0.1, 0.4]$，表示一次验证失败后保留多少信息
  - **物理意义**：$\gamma_{neg}=0.2$ 意味着在预期位置验证失败后，下一次搜索该位置的概率只有之前的 20%
  - **与稳定性的关系**：$\gamma_{neg} = 0.1 + 0.25 \times (1 - stability)$（推荐公式）
    - 稳定物体（$stability=1$）：$\gamma_{neg}=0.1$（最强惩罚）
    - 不稳定物体（$stability=0$）：$\gamma_{neg}=0.35$（较弱惩罚，给容易遮挡的物体留余地）

### 4.4.2 负向高斯核（Negative GMM）

当触发假阴性时，不仅降低该节点的评分，还向 GMM 中**注入负向高斯核**，即在降低打分的同时降低概率地图：

$$
P_{neg}(x,y) = -\alpha \cdot \mathcal{N}\left((x,y);\mu_i,\Sigma_{neg}\right)
$$

其中：
- $\alpha \in [0.3, 0.8]$：负向权重强度（与 `exist_prob` 下降幅度挂钩）
- $\Sigma_{neg}$：负向核的方差，通常大于正向核（表示"这片区域及其周边可能都没有该物体"）

**详细参数解释**：
- $\alpha$：**负向核的权重系数**，范围 $[0.3, 0.8]$
  - **建议计算方法**：$\alpha = 0.5 \times (1 - \gamma_{neg})$，表示负向惩罚与正向降权挂钩
    - 若 $\gamma_{neg}=0.15$（强惩罚），则 $\alpha = 0.5 \times 0.85 = 0.425$
    - 若 $\gamma_{neg}=0.35$（弱惩罚），则 $\alpha = 0.5 \times 0.65 = 0.325$
  - **物理意义**：$\alpha=0.5$ 表示负向核会降低该区域 50% 的原始概率（在未归一化状态下）
- $\Sigma_{neg}$：负向高斯核的协方差矩阵
  - **建议值**：$\Sigma_{neg} = (r_{neg} \times \Sigma_i)$，其中 $r_{neg} = 2.0$（负向核张开 2 倍）
  - **物理意义**：确保"已验证失败"的影响范围相对较大，不仅压低该点，也压低周边
  - 实现：若正向核方差为 $\sigma_i=0.5$ m，则负向核 $\sigma_{neg}=1.0$ m

**修正概率场**：
$$
P'(x,y) = \max\left(0, P_{original}(x,y) + P_{neg}(x,y)\right)
$$

$$
P_{final}(x,y) = \frac{P'(x,y)}{\sum_{x,y} P'(x,y) + \epsilon}
$$

**参数说明**：
- $P_{original}(x,y)$：未加负向核时的原始 GMM 概率场（第 5.2.3 节）
- $\max(0, \cdot)$：截断负值，保证概率非负
- $P'(x,y)$：加入负向核后的概率场（可能有负值）
- $P_{final}(x,y)$：最终重新归一化的概率场
- $\epsilon$：数值稳定常数，建议 $10^{-8}$

### 4.4.3 触发机制与触发条件

**何时触发负向惩罚**：
1. 视锥覆盖判定：使用碰撞检测或光线追踪判断机器人当前视野是否完全包含该节点的 `bbox_3d`
2. 置信阈值：仅当原始 $S(i) > \theta_{high}$（如 0.6）时才触发（低置信的误报不值得记仇）
3. 时间间隔：连续两次失败才记仇（避免临时遮挡导致过度反应）

**参数解释**：
- **视锥覆盖判定**：几何条件，确保机器人已足够接近且视角完整覆盖该物体的空间位置
- **置信阈值** $\theta_{high}$：触发负向惩罚的最低评分门槛
  - $\theta_{high}=0.6$：**建议值**，只有当系统对物体在该位置的置信度 > 60% 时，验证失败才会记仇
  - 物理意义：低置信度的错误预测失败不应该强烈影响下一轮（容错机制）
  - 调整策略：如果系统过于相信早期预测，降低 $\theta_{high}$；如果记仇过度，提高 $\theta_{high}$
- **连续失败条件**：需要验证失败至少 2 次才触发记仇
  - 原因：单次失败可能由遮挡、临时放置物体等造成，不必记仇
  - 实现：在节点中维护 `consecutive_miss_count`，达到 2 后触发，成功观测后重置为 0

**代码伪逻辑**：
```python
def apply_negative_feedback(obj, camera_frustum, current_gmm):
    # 判断物体是否在相机视锥内
    if is_within_frustum(obj.bbox_3d, camera_frustum):
        # 判断是否真的观测到了
        if not detect_object_in_visual(obj.category, obj.clip_embedding):
            # 触发负向惩罚
            obj.exist_prob *= GAMMA_NEG
            
            # 生成负向高斯核
            neg_gaussian = GaussianKernel(
                mu=obj.pos_2d,
                sigma=SIGMA_NEG_SCALE * obj.bbox_2d_size,
                weight=-ALPHA_NEG,
                stability_factor=obj.stability  # 稳定物更强的负向惩罚
            )
            
            # 更新 GMM
            current_gmm.negative_kernels.append(neg_gaussian)
            current_gmm.recompute_probability_map()
            
            # Agent 重评估：给出为什么没在这里
            agent_explanation = call_llm(
                f"物体 {obj.category} 在预期位置 {obj.pos} 未找到。"
                f"基于你的常识，它可能在哪里？给出 Top-3 替代位置。"
            )
            
            return agent_explanation
```

### 4.4.4 与原始 GMM 的整合

修改第 5.2 小节的 GMM 参数化，现在应该简化为：

$$
P(x,y\mid q)=\left[\sum_{i \in positive}^{N}\pi_i\,\mathcal{N}\big((x,y);\mu_i,\Sigma_i\big) + \sum_{j \in negative}^{M}\pi_j^{neg}\,\mathcal{N}\big((x,y);\mu_j,\Sigma_j^{neg}\big)\right]_{clipped}
$$

其中 $[\cdot]_{clipped}$ 表示将负值截断为 0，然后再归一化。

### 4.4.5 长期学习：共现关系的负向统计

当在某位置失败时，也应更新该位置附近共现物体的负向关系统计：

比如在"厨房桌子"位置未找到"杯子"，系统应降低"杯子与桌子共现"的 $R_{cfd}$ 权重。

在 `cooccur_stats` 中加入：
```json
{
  "positive_count": 5,     // 同时观测到的次数
  "negative_count": 2,     // 在预期位置失败次数
  "cooccur_reliability": 0.71  // = positive / (positive + negative + eps)
}
```

更新 $R_{cfd}$ 公式为：
$$
R_{cfd}(q,i) = R_{cfd}^{orig}(q,i) \times reliability_{q,i}
$$

---

## 5. 从节点分数到概率地图（GMM）

### 5.1 候选筛选
取分数前 $N$ 的候选节点，构成 $\{(\mu_i, S(i))\}_{i=1}^{N}$，其中 $\mu_i$ 为 2D 坐标。

### 5.2 GMM 参数化

#### 5.2.1 混合权重（Mixing Weights）
$$
\pi_i=\frac{\exp(S(i)/\tau)}{\sum_j \exp(S(j)/\tau)}
$$

**参数解释**：
- $S(i)$：第 $i$ 个候选节点的最终评分（来自第 4.3 节的融合结果）
- $\tau$：**温度参数（Temperature）**，控制权重分布的"锐度"
  - $\tau$ 越小（如 0.01），权重集中在最高分的节点（接近 硬选择/one-hot）
  - $\tau$ 越大（如 0.5），权重更均匀分散（平滑分布）
  - **建议值** $\tau=0.07$（参见第 7 节参数建议）
- $\epsilon$：数值稳定性常数（避免 log(0)），建议取 $10^{-8}$

**物理意义**：将节点评分转换为混合模型的权重，较高评分的节点对最终概率场的贡献更大。

---

#### 5.2.2 高斯核方差（Covariance Adaptation）
$$
\Sigma_i=\sigma_i^2 I,\quad \sigma_i=\mathrm{clip}\left(\frac{\sigma_0}{\sqrt{S(i)+\epsilon}},\sigma_{min},\sigma_{max}\right)
$$

**参数解释**：
- $\Sigma_i$：第 $i$ 个高斯核的协方差矩阵（各向同性，简化为 $\sigma_i^2 I$，即球形）
- $\sigma_0$：**基础方差尺度**（米），表示当 $S(i)=1$ 时的方差大小
  - **建议值**：$\sigma_0=1.2$ 米（环境尺度相关，需根据房间大小调整）
- $S(i)$：第 $i$ 个节点的评分，**评分越高，方差越小**（峰值越尖锐）
- $\sigma_{min}, \sigma_{max}$：**方差裁剪范围**，防止极值
  - $\sigma_{min}$：最小方差（高置信度时的"针尖"），建议值 $0.2$ 米
  - $\sigma_{max}$：最大方差（低置信度时的"大山"），建议值 $2.0$ 米
- $\mathrm{clip}(x, a, b)$：将 $x$ 限制在 $[a, b]$ 范围内，即 $\max(a, \min(x, b))$

**物理意义**：
- 高评分节点 → 方差小 → 尖圆锥 → 机器人需精确导航
- 低评分节点 → 方差大 → 平缓丘陵 → 机器人进行大范围搜索

**实现建议**：
```python
sigma_i = sigma_0 / np.sqrt(S[i] + epsilon)
sigma_i = np.clip(sigma_i, sigma_min, sigma_max)
cov_i = (sigma_i ** 2) * np.eye(2)  # 2D 情况
```

---

#### 5.2.3 概率密度（Probability Density Function）
$$
P(x,y\mid q)=\sum_{i=1}^{N}\pi_i\,\mathcal{N}\big((x,y);\mu_i,\Sigma_i\big)
$$

**参数解释**：
- $N$：候选节点数量（即取前 $N$ 高评分的节点），**建议值** $N=20$
- $\pi_i$：第 $i$ 个高斯分量的混合权重（见 5.2.1），满足 $\sum_{i=1}^{N}\pi_i=1$
- $\mathcal{N}((x,y);\mu_i,\Sigma_i)$：以 $\mu_i$ 为均值、$\Sigma_i$ 为协方差的二维高斯分布
  - $\mu_i = (x_i, y_i)$：第 $i$ 个节点的 2D 平面坐标
  - $(x,y)$：查询点的坐标
- $P(x,y\mid q)$：最终输出的空间概率密度

**物理意义**：在位置 $(x,y)$ 找到查询物体 $q$ 的概率密度。多个高斯核加权混合产生非单峰的复杂概率场。

### 5.3 可通行约束（防穿墙）
与 Occupancy Mask 相乘并归一化：
$$
P'(x,y)=\frac{P(x,y)\cdot M_{free}(x,y)}{\sum_{x,y}P(x,y)\cdot M_{free}(x,y)+\epsilon}
$$

**参数解释**：
- $P(x,y)$：原始 GMM 概率场（第 5.2.3 节）
- $M_{free}(x,y)$：**通行掩码**，二值图像
  - $M_{free}(x,y) = 1$：该位置可通行（自由空间）
  - $M_{free}(x,y) = 0$：该位置被占用（墙、障碍物）
- $M_{free}$ 的来源：从 Occupancy Grid Map、墙壁检测、或 3D 几何数据生成
- $P'(x,y)$：修正后的概率场，$\sum_{x,y} P'(x,y) = 1$（完全重新归一化）
- $\epsilon = 10^{-8}$：避免分母为零

**物理意义**：确保生成的探索点不会穿过墙壁或进入障碍物区域，增加系统的实际可行性。

### 5.4 Top-K 探索点
从 $P'$ 提取局部极大值，做最小间距抑制后输出 A/B/C。

**算法步骤**：
1. **局部极大值提取**：在 $P'$ 网格上，找出所有满足 $P'(x,y) \geq P'(x \pm 1, y \pm 1)$ 的点
2. **排序**：按概率从高到低排序，取前 $M$ 个候选点（如 $M=20$）
3. **非最大值抑制（NMS）**：消除距离过近的极大值
   - 最小间距 $d_{min}$：**建议值** $1.5$ 米，防止生成过于密集的探索点
   - 算法：从最高峰开始遍历，若与已选点距离 $< d_{min}$，则跳过
4. **输出 Top-3**：最终输出概率最高的 3 个点，标记为 A/B/C

**参数解释**：
- **$M$**：初始候选点数量，**建议值** 20，从中通过 NMS 筛选出 Top-3
- **$d_{min}$**：最小间距阈值，单位米，**建议值** 1.5 m
  - $d_{min}$ 过小 → 输出过多相邻点，浪费搜索时间
  - $d_{min}$ 过大 → 过滤掉真实的次级候选，降低覆盖
- **输出数量**：固定 Top-3（A/B/C），可在参数文件中调整

---

## 6. 最小可运行流程（MVP Pipeline）

1. 采集两期数据：$T_1, T_2$（含动态变化）  
2. 构图：生成 $G_{T_1}, G_{T_2}$  
3. 融合：得到长期图 $M_t$（含时间衰减与共现统计）  
4. 查询：计算 $S_{rag}$ 与 $S_{agent}$，再动态融合  
5. 概率化：GMM + 通行掩码，输出 Top-3 候选点

---

## 7. 参数建议（第一版可直接用）

以下为所有关键参数的统一建议值及其调整策略。

### 7.1 图匹配参数（第 3 节）
- **匹配融合系数**：$\eta_1=0.4$（空间IoU），$\eta_2=0.4$（特征相似度），$\eta_3=0.2$（距离惩罚）
  - 调整：若误匹配↑，增大 $\eta_2$；若遗漏↑，减小 $\eta_3$
- **匹配阈值**：$\tau_{match}=0.55$（后续网格搜索 [0.45, 0.65]）

### 7.2 时间衰减参数（第 3 节）
- **半衰期**：$T_{half}=3$ 天（环境情景决定，静态家居可用 7 天，高动态办公室用 1 天）
- **衰减系数**（若时间单位为秒）：$\lambda = \frac{\ln 2}{3 \times 86400} \approx 2.67 \times 10^{-6}$ s$^{-1}$
- **衰减系数**（若时间单位为天）：$\lambda = \frac{\ln 2}{3} \approx 0.231$ day$^{-1}$

### 7.3 RAG 评分参数（第 4.1 节）
- **融合系数**：$\beta_1=0.65$（语义相似度），$\beta_2=0.35$（关联置信度）
- **稳定性抗衰减系数**：$\kappa=1.0$（搜索范围 [0.5, 2.0]，高动态环境用 2.0）
- **小数值常数**：$\epsilon=10^{-8}$（数值稳定性）
- **关联置信最小共现计数**：`min_cooccur_count=2`（低于此阈值降低 $R_{cfd}$ 置信）

### 7.4 Agent 权重参数（第 4.3 节）
- **权重衰减斜率**：$k=0.35$（探索 3 轮后权重开始明显转移）
- **最小 Agent 权重**：$w_{min}=0.15$（15% 的权重始终留给常识先验）

### 7.5 负向 GMM 参数（第 4.4 节）
- **负向衰减系数**（依赖稳定性）：$\gamma_{neg} = 0.1 + 0.25 \times (1 - stability)$
  - 高稳定物体（椅子、桌子）：$\gamma_{neg} \approx 0.18$
  - 中等物体（包、手机）：$\gamma_{neg} \approx 0.25$
  - 低稳定物体（小物件）：$\gamma_{neg} \approx 0.35$
- **负向核权重**：$\alpha = 0.5 \times (1 - \gamma_{neg})$
- **负向核方差缩放**：$r_{neg}=2.0$（负向核张开 2 倍正向核）
- **置信阈值**（触发记仇）：$\theta_{high}=0.6$

### 7.6 GMM 参数（第 5 节）
- **温度参数**（softmax）：$\tau=0.07$（集中权重，搜索范围 [0.03, 0.15]）
- **候选节点数**：$N=20$（初筛，极端环境可调 [10, 50]）
- **基础方差**：$\sigma_0=1.2$ 米（房间依赖，开放空间 1.5m，狭小空间 0.8m）
- **方差裁剪范围**：$\sigma_{min}=0.2$ m，$\sigma_{max}=2.0$ m
- **NMS 最小间距**：$d_{min}=1.5$ 米（输出 Top-3 探索点时的最小分离距离）
- **输出候选点数**：Top-3（标记为 A/B/C）

### 7.7 流程配置（第 8 节）
- **数值稳定常数**：全局使用 $\epsilon=10^{-8}$
- **评分缩放（可选）**：若评分范围异常，可用 min-max 归一化到 [0,1]

### 7.8 参数调优方向

**高成功率场景**（已验证环境，物体位置相对稳定）：
- 增大 $\kappa=1.5$（更信任稳定物体的历史信息）
- 减小 $\tau=0.05$（集中 GMM 权重）
- 增大 $w_{min}=0.2$（保留更多常识）

**高动态场景**（物体频繁挪动，环境变化快）：
- 减小 $T_{half}=1$ 天（快速遗忘过时信息）
- 增大 $\tau=0.1$（平滑 GMM，多个候选点概率接近）
- 减小 $w_{min}=0.10$（快速倾向相信新探索数据）
- 增大 $\gamma_{neg}=0.40$（减弱负向惩罚，容错高）

**计算受限场景**（实时性要求高）：
- 减小 $N=10$（减少候选节点）
- 增大 $d_{min}=2.0$（输出更稀疏的点）
- 简化 $R_{cfd}$（仅用最近 K=3 个扫描累积共现）

---

## 8. 成功判据（与论文实验一致）

### 8.1 功能判据
- 能输出概率图而非单点
- 动态物体在 $T_2$ 时，峰值从旧位置迁移到新位置/高可能邻域
- 输出 Top-3 候选点，并可解释每点的打分分解

### 8.2 对比判据（消融）
- 无 Agent：动态场景下成功率明显下降
- 无视觉特征：同类物体区分能力下降
- 无 $R_{cfd}$：邻域引导能力下降
- 无时间衰减：对过时记忆过度依赖

---

## 9. 代码落地映射（你仓库的直接改造点）

建议先改这几处（按优先级）：
1. `AgenticRAG/semantic_map/object.py`：补齐 `stability`、`last_update_time`、`cooccur_stats`、`exist_prob` 字段
2. `AgenticRAG/semantic_map_Update/object_Update.py`：实现时间差衰减更新与缺失观测逻辑
3. `AgenticRAG/semantic_map_Update/map_Update.py`：加入跨时刻匹配（3D IoU + 视觉相似）
4. `AgenticRAG/GMM_map_Create/agent_Rscore.py`：统一实现 $S_{rag}$、$S_{agent}$、动态融合
5. `AgenticRAG/GMM_map_Create/GMM_map_calcualte.py`：实现 occupancy mask 约束与 Top-K 提取

---

## 10. 后续扩展（下一阶段）

当你确认本规范后，再进入：
- 概率点到可导航目标点映射
- 到点失败后的局部图更新与再检索（闭环）
- SR / SPL / NDTW 统一评测脚本

---

## 附录 A：参数速查索引

本表让你快速定位每个参数的定义、建议值和调整位置。

| 符号 | 中文名 | 定义位置 | 建议值 | 调整影响 |
|-----|-------|--------|-------|--------|
| $\eta_1, \eta_2, \eta_3$ | 匹配融合系数 | 3.1 | 0.4, 0.4, 0.2 | 误匹配/遗漏比例 |
| $\tau_{match}$ | 匹配阈值 | 3.1 | 0.55 | 节点重复度 |
| $\lambda$ | 时间衰减系数 | 3.2 | 0.231/day | 物体遗忘速度 |
| $T_{half}$ | 时间半衰期 | 3.2 | 3 days | 物体有效期 |
| $\Delta T_i$ | 时间差 | 3.2 | 实时计算 | N/A |
| $w_t(i)$ | 时间权重 | 3.2 | $\exp(-\lambda \Delta T)$ | N/A |
| $R_{sim}(i)$ | 语义相似度 | 4.1 | $\cos(f_q, f_i)$ | 特征匹配能力 |
| $R_{time}(i)$ | 时间可信度 | 4.1 | $\exp(-\lambda \Delta T)$ | 时间影响 |
| $R_{stab}(i)$ | 稳定性指标 | 4.1 | 由 Agent 赋予 | 物体动态性评估 |
| $\kappa$ | 稳定性抗衰减系数 | 4.1 | 1.0 | [0.5-2.0] 调整 |
| $\epsilon$ | 数值稳定常数 | 4.1, 5.2 | $10^{-8}$ | 不需调整 |
| $R_{cfd}(q,i)$ | 关联置信度 | 4.1 | 计算公式 | 上下文引导 |
| $\beta_1, \beta_2$ | RAG 融合系数 | 4.1 | 0.65, 0.35 | 语义 vs 关联权重 |
| $S_{base}(i)$ | RAG 基础分 | 4.1 | 加权融合 | N/A |
| $S_{rag}(i)$ | RAG 最终分 | 4.1 | $S_{base} \cdot G_{ts}$ | N/A |
| $G_{ts}(i)$ | 时间-稳定门控 | 4.1 | 指数门控 | N/A |
| $S_{agent}(i)$ | Agent 补充分 | 4.2 | LLM 输出 | 先验知识质量 |
| $n$ | 探索轮次 | 4.3 | 实时计数 | N/A |
| $w_{agent}(n)$ | Agent 权重 | 4.3 | $\max(w_{min}, \exp(-kn))$ | N/A |
| $w_{rag}(n)$ | RAG 权重 | 4.3 | $1 - w_{agent}$ | N/A |
| $k$ | 权重衰减斜率 | 4.3 | 0.35 | 信息转移速度 |
| $w_{min}$ | 最小 Agent 权重 | 4.3 | 0.15 | 先验保留比例 |
| $S(i)$ | 最终融合分 | 4.3 | $w_{rag} \cdot S_{rag} + w_{agent} \cdot S_{agent}$ | N/A |
| $\gamma_{neg}$ | 负向衰减系数 | 4.4.1 | $0.1 + 0.25(1-stability)$ | 记仇强度 |
| $\alpha$ | 负向核权重 | 4.4.2 | $0.5(1-\gamma_{neg})$ | 惩罚影响范围 |
| $\Sigma_{neg}$ | 负向核方差 | 4.4.2 | $r_{neg}^2 \Sigma_i$, $r_{neg}=2.0$ | 惩罚扩散 |
| $\theta_{high}$ | 记仇置信阈值 | 4.4.3 | 0.6 | 什么时候才记仇 |
| $P_{neg}(x,y)$ | 负向高斯核 | 4.4.2 | $-\alpha \mathcal{N}(...)$ | N/A |
| $M_{free}(x,y)$ | 通行掩码 | 5.3 | 外部输入 | 路径约束 |
| $\pi_i$ | GMM 混合权重 | 5.2.1 | softmax 归一化 | N/A |
| $\tau$ | 温度参数 | 5.2.1 | 0.07 | [0.03-0.15] 权重集中度 |
| $N$ | 候选节点数 | 5.2.3 | 20 | [10-50] 计算量 |
| $\Sigma_i$ | 高斯核协方差 | 5.2.2 | $\sigma_i^2 I$ | N/A |
| $\sigma_i$ | 高斯核标准差 | 5.2.2 | $\mathrm{clip}(\sigma_0/\sqrt{S+\epsilon}, \sigma_{min}, \sigma_{max})$ | N/A |
| $\sigma_0$ | 基础方差尺度 | 5.2.2 | 1.2 m | [0.8-1.5] 房间依赖 |
| $\sigma_{min}$ | 最小方差 | 5.2.2 | 0.2 m | 最锐高斯 |
| $\sigma_{max}$ | 最大方差 | 5.2.2 | 2.0 m | 最平缓高斯 |
| $d_{min}$ | NMS 最小间距 | 5.4 | 1.5 m | 输出点的稀疏度 |

---

## 附录 B：常见问题解答（FAQ）

### Q1: 如何判断 $\tau_{match}$ 是否合适？
**A:** 运行图融合后，检查节点数量增长：
- 若节点数量增长过快 → 误匹配多 → 提高 $\tau_{match}$ 至 0.65
- 若节点数量增长缓慢或不增长 → 遗漏多 → 降低 $\tau_{match}$ 至 0.45

### Q2: 为什么要调整 $\kappa$？
**A:** $\kappa$ 控制稳定性对时间衰减的保护力度：
- 低动态环境（家居、办公室）：$\kappa=1.0$（平衡）
- 高动态环境（人群密集区）：$\kappa=2.0$（稳定物更长效）

### Q3: 如果物体频繁挪动，应该改什么参数？
**A:** 改 $T_{half}$ 和 $w_{min}$：
- $T_{half}$：从 3 天改为 1 天（快速遗忘）
- $w_{min}$：从 0.15 改为 0.10（更信任新探索）
- 其他不变

### Q4: "记仇"机制（负向 GMM）有什么风险？
**A:** 主要风险是过度惩罚：
- 若物体暂时被遮挡（如被人挡住），可能误触发记仇
- 解决：增大 $\theta_{high}$ 至 0.7（只有高置信才记仇）
- 或调整 `consecutive_miss_count` 从 2 改为 3（需连续 3 次失败）

### Q5: GMM 输出只有 1 个峰值怎么办？
**A:** 增大 $\tau$ 值，从 0.07 改为 0.12，让 GMM 权重分布更均匀，多个候选点概率接近。

---

## 附录 C：代码实现检查清单

实现所有数学模型前，确保以下数据结构已准备：

- [ ] Object 节点包含所有字段：`obj_id`, `category`, `clip_embedding`, `pos_3d`, `bbox_3d`, `last_update_time`, `stability`, `cooccur_stats`, `exist_prob`, `consecutive_miss_count`
- [ ] 图融合支持 3D IoU 计算且结果范围 $[0,1]$
- [ ] 特征相似度计算使用归一化的 CLIP 向量（或确保值域为 $[-1,1]$）
- [ ] 时间戳格式统一为 ISO 8601（如 `2026-03-26T10:00:00Z`）
- [ ] 所有浮点计算使用 `float64` 确保数值精度
- [ ] GMM 拟合前验证候选点坐标在 Occupancy Grid 范围内
- [ ] NMS 实现了欧氏距离计算且单位与 $d_{min}$ 一致
- [ ] 最终概率场经过归一化，$\sum P = 1$（或接近 1，误差 < $10^{-6}$）

---

## 附录 D：全文参数依赖关系图

```
输入: 查询 q, 当前图 G_t, 历史图 M_{t-1}, 探索次数 n
  ↓
[图融合] τ_match, η_1/2/3, λ, ΔT
  ↓ 输出: 融合图 M_t，每个节点有 exist_prob, stability
  ↓
[RAG评分] β_1/2, κ, ε, τ/stability
  ↓ 输出: S_rag(i)
  ↓
[Agent评分] LLM调用
  ↓ 输出: S_agent(i)
  ↓
[权重融合] w_agent(n), w_rag(n), k, w_min
  ↓ 输出: S(i) 最终分数
  ↓
[负向修正] γ_neg, α, Σ_neg, θ_high（可选）
  ↓ 输出: S(i) 调整后分数
  ↓
[GMM参数化] τ, N, σ_0, σ_min/max
  ↓ 输出: π_i, Σ_i
  ↓
[概率场生成] 高斯混合
  ↓ 输出: P(x,y|q)
  ↓
[通行约束] M_free(x,y), ε
  ↓ 输出: P'(x,y) 归一化
  ↓
[NMS提取] d_min, Top-K
  ↓ 输出: A/B/C 三个探索点
```

---

## 一句话总结（再次强调）
你的系统不是“记住物体坐标”，而是“在动态世界中维护可更新的长期语义记忆，并输出可行动的概率信念图”。这就是你论文最强的叙事与工程落点。
