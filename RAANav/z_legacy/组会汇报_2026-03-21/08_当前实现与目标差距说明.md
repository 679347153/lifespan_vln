# 当前实现与最终目标差距说明（组会答疑版）

## 你现在已经实现了什么（可验证）
- 已完成 Habitat 场景采样：拿到 `rgb/depth/semantic/pose` 四类时序数据（60帧）。
- 已完成 HOV-SG 低算力最小运行：拿到对象点云与特征产物（`objects/*.ply`, `mask_feats.pt`, `full_pcd.ply`）。
- 已完成数据转换：把 HOV-SG 产物转为 AgenticRAG 可读 `map_history.json`。
- 已完成合并链路健康验证：`map_Update.py` 能正确读取并输出 merged map。
- 已完成 3D 交互展示：可在浏览器交互查看原始房间点云与对象提取结果。

## 你现在“还没完成”的部分（离最终目标差距）
- 还没有稳定产出 HOV-SG 层级图 `graph/floors/rooms/objects/*.json`（低算力+场景条件下上游分割不稳定）。
- 还没有 T2 动态扰动后的完整对比实验（T1/T2 热力图与Top-K）。
- 还没有完成 `agent_Rscore -> GMM` 的端到端实验展示图（当前仅到 map 转换和合并验证）。
- 当前转换对象标签是 `unknown_object` 占位，尚未接入语义命名策略。

## 你现在最关键的一步（下一步）
- 构造 T2（对 T1 做“删除/位移”扰动）并跑通：
  `map_Update -> agent_Rscore -> GMM`。

## 组会可用的一句话定位
- 我们已经打通了“仿真采集到可用语义地图”的最小闭环，并完成可交互3D可视化；当前主要差距集中在“动态对比实验结果图（T1/T2）与完整评分热力图展示”。
