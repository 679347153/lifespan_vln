# 组会汇报摘要（2026-03-21）

## 1. 本周目标
- 在低算力条件下，打通“Habitat 仿真采样 -> HOV-SG 产物 -> AgenticRAG 可读地图 -> 地图合并”最小可验证链路。
- 保证外部数据目录只读，不直接改动 `/home/adminer/seap_test/data`。

## 2. 已完成工作
- 数据准备：已将场景 `00824-Dd4bFSTQ8gi` 从只读目录复制到工作区 `experiment_data/hm3d/val/`。
- Habitat 采样：成功生成 60 帧 `rgb/depth/semantic/pose`（用于 T1）。
- HOV-SG 低算力运行：
  - 完整层级图模式在当前参数下存在上游索引越界风险；
  - 已切换到稳定“最小可验证模式”（仅特征与对象点云产物），成功产出：`full_feats.pt`、`mask_feats.pt`、`full_pcd.ply`、`objects/*.ply`。
- 数据转换：新增最小 Adapter，将 HOV-SG 产物转换为 AgenticRAG 格式 `map_history.json`。
- 合并验证：`map_Update.py` 已成功读取并合并转换后地图，输出 `map_merged_hovsg_minimal.json`。

## 3. 关键结果（可量化）
- 采样文件计数：`rgb/depth/semantic/pose = 60/60/60/60`。
- 转换后对象数：`12`。
- 合并后关键字段覆盖（对象级）：`clip_embedding`、`last_update_time`、`exist_prob`、`cooccur_stats` 全部可用。

## 4. 可视化（组会可直接展示）
- 采样计数图：`01_采样数据计数.png`
- T1 对象区域 BEV：`02_T1对象区域可视化.png`
- 合并图关键字段覆盖：`03_关键字段覆盖率.png`

## 5. 工程决策说明（为何这样做）
- 低算力优先“可验证闭环”，因此默认先不做层级图分割（`pipeline.create_graph=false`），避免上游 floor/room 分割不稳定导致链路中断。
- 当前阶段核心目标是保证：数据可采、可转、可并、可进入评分/GMM。

## 6. 当前风险与限制
- 由于 `create_graph=false`，暂不产出稳定 floor/room/object 层级 JSON，只保留对象点云与特征产物。
- Adapter 当前使用 `unknown_object` 标签占位；后续需要接入对象语义命名策略（可由 CLIP 文本匹配或外部标签映射补齐）。

## 7. 下周（下一步）计划
- 生成 T2（对 T1 做目标对象扰动：删除/位移）并形成 `map_now.json`。
- 跑完整链路：`map_Update -> agent_Rscore -> GMM`，输出 Top-K 概率点。
- 产出第一版对比图：T1/T2 热力分布变化与关键分数拆解。
