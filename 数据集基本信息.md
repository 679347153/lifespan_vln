**数据集定位**
这个项目构建的是一个面向“动态家庭物体布局变化”的 embodied navigation benchmark。它不是只给一个静态 HM3D/Habitat 场景放一组物体，而是针对同一个家庭场景、同一批可移动物体，批量生成多个合理但不同的物体布局，用来模拟真实家庭中物体随时间、人类活动和使用习惯发生变化的状态。

可以把每个生成出的 `layout` 理解成同一个家庭在某个时间点的快照。例如同一个场景 `00808-y9hTuugGdiq`，在 `layout_000_seed_42` 中花瓶可能在柜面上，在 `layout_001_seed_43` 中棋盘可能换到桌面，闹钟可能出现在另一个合适房间。benchmark 再基于这些多时间布局生成类似 ObjectNav 的连续导航 episode。

**核心目标**
数据集主要覆盖四类能力：

1. 长期动态环境理解：同一场景中物体位置随时间变化。
2. 语义合理性：物体应该出现在合理房间，例如茶具更可能在餐厅/厨房，闹钟更可能在卧室。
3. 几何合理性：物体最终应落在合理承载面上，例如桌面、架子、柜面、床面、沙发、地面等。
4. 多模态目标导航：episode 同时包含开放词汇、图像目标和语言目标三类子任务。

**数据生成链路**
整体链路分为“布局生成”和“benchmark episode 生成”两层。

第一层是 layout 生成：

```text
HM3D 场景
-> export_scene_info.py 提取场景语义、房间、物体实例
-> query_rooms_for_objects.py 为每个待放置物体生成房间推荐
-> sample_and_place_objects.py 按概率采样物体所在房间
-> query_room_receptacle_objects.py 提取每个房间可承载 instance 的 top surface
-> assign_objects_to_receptacle_instances.py 分配物体到具体承载实例
-> place_objects_on_instances.py 执行几何放置和碰撞检查
-> batch_generate_layouts.py 批量生成多个时间状态 layout
```

第二层是 benchmark 构建：

```text
batch layout manifest
-> benchmark.build_episodes 生成 episode JSON
-> benchmark.runner 调用 agent 执行 episode，输出 trajectory JSONL
-> benchmark.evaluate 离线计算 SR/SPL/探索提升曲线
```

**原始和中间数据**
项目中会产生多类中间文件，每类都有明确用途：

- 场景信息：`results/scene_info/<scene>/<scene>_scene_info.json`
  包含场景名、房间列表、语义类别、实例几何信息等。

- 房间推荐：`results/scene_info/<scene>/<object>_rooms.json`
  每个物体对应一个房间推荐结果，来自 Qwen3-VL 或启发式逻辑。

- 房间概率：`results/probabilities/<scene>/<object>_probs.json`
  将房间推荐转成可复用的概率分布，后续批量生成 layout 时只需要重新 sample。

- 承载面结果：`results/receptacle_queries/<scene>/<scene>_receptacle_surfaces_all_rooms.json`
  包含每个房间可放置实例、承载面高度、点云文件、面积、跨度、法向、置信度等。

- 分配计划：`results/object_instance_assignments/<scene>/*_object_instance_plan.json`
  记录每个物体被分配到哪个房间、哪个承载 instance，以及备用 instance。

- 最终布局：`results/layouts/<scene>/batch_<time>/layout_*_seed_*.json`
  这是 benchmark episode 的直接来源。

**最终 layout 内容**
每个最终 layout JSON 表示一个时间状态，核心包含：

- `scene`：对应 HM3D 场景。
- `objects`：当前时间状态下所有已成功放置的物体。
- 每个 object 通常包含：
  - `id`
  - `model_id`
  - `name`
  - `position`
  - `rotation`
  - `sampled_region_id`
  - `target_instance_id`
  - `placement_y_offset`
  - `placement_target_source`
  - `placement_radius`
  - `object_profile`
- `auto_placement_stats`：
  - `total_objects`
  - `placed_count`
  - `failed_count`
  - `failed_by_reason`
  - `failed_objects`
  - `profile_diagnostics`
- `batch_generation`：
  - `batch_id`
  - `layout_index`
  - `seed`
  - `sampled_object_count`
  - `assignment_count`
  - `placement_retry`

这些字段让 layout 不只是“物体坐标文件”，还保留了放置来源、失败原因、物体尺寸估计、retry 记录等审计信息。

**物体 profile 和承载面合理性**
项目通过 `object_profiles.py` 维护统一的物体几何和放置类别信息。每个物体可以有：

- `radius`
- `footprint_x`
- `footprint_z`
- `height`
- `y_offset`
- `placement_class`

典型 `placement_class` 包括：

- `floor_only`：轮椅、桌子、椅子等基本只能放地面。
- `small_tabletop`：闹钟、相机、花瓶等适合桌面/柜面/架子。
- `large_tabletop`：棋盘、茶具等需要较大承载面。
- `soft_surface`：枕头等适合床、沙发、椅子或地毯。
- `floor_or_large_surface`：植物等可放地面或大平面。

如果有更精确尺寸，可以通过 `object_profiles.json` 覆盖默认估计。

**Episode 数据内容**
`benchmark.build_episodes` 会从 batch layout 生成 episode。每个 episode 是一个连续任务序列，包含 5-10 个 subtask。

Episode 核心字段包括：

- `episode_id`：唯一 ID。
- `split`：`train`、`val` 或 `test`。
- `scene_name`：HM3D 场景名。
- `layout_id`：对应的时间状态，例如 `layout_000_seed_42`。
- `state_index`：该 layout 在同一场景时间序列中的顺序。
- `seen_layout_count_before`：agent 在运行当前 episode 前见过多少个 layout，用于衡量长期探索收益。
- `scene_state.layout_path`：源 layout JSON。
- `start_pose`：起点位姿。
- `max_steps`：最大步数预算。
- `subtasks`：连续子任务列表。

每个 subtask 包含：

- `subtask_id`
- `task_type`
- `target_object`
- `target_object_id`
- `target_position`
- `prompt`
- `image_path`
- `language_prompt`
- `success_radius`
- `metadata`

**三类子任务**
每个 episode 会尽量平衡三类任务数量，三类数量差不超过 1：

- `open_vocab`
  开放词汇目标，例如：
  ```text
  Find alarm_clock_01.
  ```

- `image_goal`
  图像目标，使用 `objects_images/<object_stem>.*` 中的参考图像，例如：
  ```text
  Find the object shown in the reference image: Camera_01.
  ```

- `language_goal`
  模板化语言目标，结合 layout 中的房间和承载实例信息，例如：
  ```text
  Go to the tea_set_01, which is usually placed near receptacle instance 584 in region 13.
  ```

`image_goal` 必须能在 `objects_images` 下找到对应参考图，否则生成器会报错，避免生成不可执行任务。

**评测数据**
agent 运行后，`benchmark.runner` 输出 trajectory JSONL。每行对应一个 episode，包含：

- `episode_id`
- `agent_id`
- `scene_name`
- `layout_id`
- `seen_layout_count_before`
- `steps`
- `subtask_traces`
- 每个 subtask 的：
  - `final_pose`
  - `path_length`
  - `steps`
  - `elapsed_seconds`

评测器不会相信 agent 自报 success，而是读取每个 subtask 的 `final_pose`，计算它到 `target_position` 的距离。如果距离小于等于 `success_radius`，该 subtask 成功。只有 episode 内全部 subtask 成功，整个 episode 才算成功。

**主要指标**
输出目录通常是：

```text
benchmark/eval/<version>/
```

包含：

- `summary.json`
  episode-level 主指标：
  - success rate
  - SPL
  - average path length
  - average shortest path length
  - average steps
  - dynamic/fixed memory accuracy

- `episode_results.jsonl`
  每个 episode 的详细结果。

- `by_task_type.json`
  按任务类型分组的指标：
  - `open_vocab`
  - `image_goal`
  - `language_goal`
  - `all`

- `exploration_curve.json`
  按 `seen_layout_count_before` 聚合的长期探索提升曲线。

SPL 公式为：

```text
SPL = success * shortest_path_sum / max(actual_path_length, shortest_path_sum)
```

其中 `shortest_path_sum` 是从 episode 起点依次到所有子任务目标的最短路径总和。

**长期探索含义**
这个 benchmark 的一个重点是 `seen_layout_count_before`。同一 scene 下：

- 第 0 个 layout 是 baseline。
- 第 k 个 layout 代表 agent 已经历过更多时间状态。
- 评测会比较不同 `seen_layout_count_before` 下的 SR/SPL。

探索提升指标包括：

```text
SR_gain_pct = (SR_k - SR_0) / max(SR_0, 1e-6) * 100
SPL_gain_pct = (SPL_k - SPL_0) / max(SPL_0, 1e-6) * 100
```

这能用于研究：agent 是否能随着见过更多家庭状态，逐渐更高效地找到动态变化的物体。

**Habitat-Sim 与 fallback**
在完整 Habitat 环境中，`HabitatLayoutAdapter` 可以：

- 解析 HM3D scene。
- 加载 layout 中的 objects。
- 使用 `habitat_sim.PathFinder` 采样可导航起点。
- 计算 geodesic shortest path。

在普通 Python 环境中，如果没有 Habitat-Sim，也可以使用 Euclidean fallback 完成：

- schema 检查
- episode 生成
- trajectory 格式验证
- 指标聚合 smoke test

如果希望强制使用 Habitat-Sim，可以使用 `--no-euclidean-fallback`。

**典型目录结构**
最终形成的数据集大致包括：

```text
results/
  scene_info/<scene>/
    <scene>_scene_info.json
    <object>_rooms.json

  probabilities/<scene>/
    <object>_probs.json

  receptacle_queries/<scene>/
    <scene>_receptacle_surfaces_all_rooms.json
    surface_pointclouds/*.ply

  object_instance_assignments/<scene>/
    *_object_instance_plan.json

  layouts/<scene>/batch_<YYYYmmdd_HHMMSS>/
    manifest.json
    layout_000_seed_42.json
    layout_001_seed_43.json
    ...

benchmark/
  episodes/<version>/<split>/<scene>/<layout_id>/
    *.json

  splits/
    benchmark_split_<version>.json

  eval/<version>/
    summary.json
    episode_results.jsonl
    by_task_type.json
    exploration_curve.json
```

**适合评估的研究问题**
这个数据集适合评估：

- 动态 ObjectNav
- 开放词汇目标导航
- 图像目标导航
- 语言目标导航
- 长期家庭环境记忆
- 物体位置变化后的重定位能力
- 多子任务连续导航
- agent 是否能利用历史 layout 提升搜索效率
- 语义先验和几何先验对导航表现的影响

简言之，这个 benchmark 的核心价值是：把“家庭物体会变化”这件真实世界常识，转化成一套可批量生成、可审计、可复现、可自动评测的 embodied AI 数据集。