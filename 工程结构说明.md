# 工程结构说明

本文档用于说明当前工程中各文件夹和重要代码文件的作用，帮助后续维护、复现实验和继续完善“同一家庭环境下的长期能力”评测系统。

## 1. 项目总体定位

本工程围绕动态家庭环境中的长期具身导航与检索任务展开，核心目标是：

- 基于 HM3D 家庭场景构造动态 layout。
- 使用 `objects/` 与 `objects_images/` 中的物体资产和图像构造目标。
- 在 `benchmark/` 中生成和评测长期任务集。
- 使用 `RAANav/AgenticRAG/` 中的 Agentic-RAG 方法进行闭环导航、记忆检索、CLIP/GMM/Agent 融合评分。
- 使用 `remote_vision_server/` 将 GroundingDINO、MobileSAM、CLIP 等视觉模型放到远程 GPU 服务器上运行，工作站只负责 Habitat 仿真和调用。

## 2. 顶层目录

### `results/`

数据集生成与动态 layout 相关的主要结果目录。当前数据集的大部分结构化中间结果和 layout 文件都在这里。

常见子目录：

- `results/scene_info/`：场景级信息导出结果，包括房间、物体、语义信息等。
- `results/probabilities/`：物体出现在不同房间或位置上的概率结果。
- `results/receptacle_queries/`：承载面、可放置区域、房间内可交互对象等查询结果。
- `results/object_instance_assignments/`：动态物体实例到房间或承载面的分配结果。
- `results/layouts/`：生成的动态布局结果，是长期任务中不同 `layout_id`、`state_index` 的重要来源。
- `results/room_instances/`：房间实例、房间类型和相关结构化信息。

### `hm3d/`

HM3D 场景数据目录，是 Habitat 仿真和 benchmark 评测的原始场景来源。

常见内容：

- `hm3d/minival/`：小规模验证场景。
- `hm3d/val/`：验证集场景。
- `hm3d/*_scene_dataset_config.json`：Habitat-Sim 加载场景所需的数据集配置文件。
- `hm3d/scene_info_export/`：部分场景信息导出结果。

### `objects/`

动态 layout 中使用的 3D 物体资产目录。

通常每个物体以 `.gltf` 资产目录形式存在，并带有 `textures/` 等纹理资源。它们用于在同一 HM3D 家庭环境中生成不同时间状态下的物体摆放。

### `objects_images/`

物体图像目录，主要用于：

- image-goal 任务目标。
- Qwen-VL 或其他 VLM 对物体外观的理解。
- CLIP 图像索引和图像检索。
- 动态物体与语义标签之间的辅助对齐。

### `benchmark/`

任务集构建、划分、运行和评测目录。长期能力评测主线应以该目录为中心。

重要文件：

- `benchmark/README.md`：benchmark 使用说明。
- `benchmark/schemas.py`：episode、subtask、layout 等数据结构定义。
- `benchmark/build_episodes.py`：根据动态 layout 和数据集信息构建任务 episode。
- `benchmark/runner.py`：benchmark 运行入口或调度逻辑。
- `benchmark/evaluate.py`：对轨迹和任务结果进行评测。
- `benchmark/metrics.py`：SR、SPL、found rate 等指标计算逻辑。
- `benchmark/habitat_adapter.py`：Habitat-Sim 路径、距离、场景加载适配层。

重要子目录：

- `benchmark/episodes/`：生成后的 episode JSON 文件。
- `benchmark/splits/`：训练、验证、测试划分文件，例如 `benchmark_split_longterm_v1.json`。
- `benchmark/eval/`：评测输出结果目录。

### `RAANav/`

RAANav 与 Agentic-RAG 算法主体目录。

重要子目录：

- `RAANav/AgenticRAG/`：当前 Agentic-RAG 代码主体。
- `RAANav/计划/`：阶段计划、执行报告、环境构建说明、问题记录等文档。
- `RAANav/z_legacy/`：历史代码或旧版本实现。
- `RAANav/重要信息备份/`：重要中间信息和备份材料。

### `remote_vision_server/`

远程视觉模型服务目录，用于把重模型放在服务器 GPU 上运行，工作站通过 HTTP 或 SSH tunnel 调用。

重要文件：

- `remote_vision_server/server.py`：FastAPI 服务端，提供 GroundingDINO、MobileSAM、CLIP embedding 和 CLIP 文本-图像相似度接口。
- `remote_vision_server/client.py`：工作站侧 Python 客户端，被导航代码调用。
- `remote_vision_server/test_client.py`：远程服务连通性和功能测试脚本。
- `remote_vision_server/check_server_env.py`：服务器环境检查脚本。
- `remote_vision_server/run_server.sh`：服务器侧启动脚本。
- `remote_vision_server/workstation_tunnel.sh`：工作站侧 SSH tunnel 辅助脚本。
- `remote_vision_server/README.md`：远程视觉服务部署和调用说明。
- `remote_vision_server/REMOTE_CLIP_EMBEDDING.md`：远程 CLIP embedding 和检索说明。
- `remote_vision_server/GROUNDINGDINO_TORCH_API_FIX.md`：GroundingDINO 与 PyTorch API 兼容问题记录。
- `remote_vision_server/WORKSTATION_CLIP_FIX.md`：工作站侧 CLIP 离线/远程化问题记录。

### `scripts/`

根目录下的数据集和 benchmark 辅助脚本。

重要文件：

- `scripts/01_prepare_splits.py`：准备 benchmark 数据划分。
- `scripts/03_generate_benchmark_episodes.py`：生成 benchmark episode。
- `scripts/04_validate_episodes.py`：校验 episode 合法性。
- `scripts/05_evaluate_benchmark.py`：运行 benchmark 评测。

### `__pycache__/`

Python 缓存目录，可再生成，不属于核心源代码。

## 3. 根目录重要文件

### `数据集基本信息.md`

数据集说明文档。它解释当前数据集的来源、目录结构、动态 layout、任务集字段、评测目标等信息，是理解 `results/`、`hm3d/`、`objects/`、`objects_images/` 和 `benchmark/` 的首要入口。

### `AgenticRAG_可执行技术规范_v1.md`

Agentic-RAG 方案的可执行技术规范，描述方法设计、模块职责、检索与决策流程、地图表示、融合评分等内容。

### `hm3d_paths.py`

HM3D 场景路径解析工具，用于在不同机器、不同目录结构下定位场景文件和数据集配置。

### `log_filter.py`

命令运行和日志过滤辅助脚本。常用于包装较长命令，减少 Habitat、Hydra、模型加载等库产生的噪声日志。

### `.gitignore`

Git 忽略规则，控制缓存、模型权重、生成结果等文件是否进入版本管理。

## 4. `RAANav/AgenticRAG/` 目录

这是当前算法系统的主体目录。

### `RAANav/AgenticRAG/benchmark_adapter/`

连接 benchmark episode 与 Agentic-RAG 闭环执行的适配层。

重要文件：

- `run_closed_loop.py`：长期 benchmark 闭环运行入口，将 episode 转换为 Agentic-RAG 查询、候选、轨迹和评测输出。
- `formal_scoring.py`：正式 Agent/RAG 融合评分逻辑，支持 memory、time decay、co-occurrence、negative feedback 等因素。
- `memory_builder.py`：构建 scene-level 或 layout-level 记忆。
- `dataset_index.py`：索引数据集中的 scene、layout、episode 等信息。
- `layout_to_floors.py`：将动态 layout 转换为 Agentic-RAG 可使用的楼层、房间、物体结构。
- `episode_to_queries.py`：将 benchmark episode 中的任务转换为检索查询。
- `image_goal_index.py`：image-goal 图像索引与图像目标处理。
- `offline_eval.py`：离线评测和 episode 加载工具。
- `diagnose_nav_smoke.py`：对一次导航输出进行诊断，检查目标、候选、CLIP 命中、GMM 概率场等是否有效。

### `RAANav/AgenticRAG/semantic_map/`

语义地图基础数据结构。

重要文件：

- `floor.py`：楼层结构。
- `room.py`：房间结构。
- `object.py`：物体结构。
- `embedding_io.py`：embedding 读写工具。

### `RAANav/AgenticRAG/semantic_map_Create/`

语义地图构建模块。

重要文件：

- `map_Create.py`：地图创建主流程。
- `scene_extract.py`：从场景中抽取结构化信息。
- `occupancy_grid.py`：从 navmesh 或深度观测构建占据栅格。
- `room_segmentation.py`：房间分割。
- `voronoi_graph.py`：Voronoi 图构建，用于导航候选和路径规划。
- `perception.py`：感知结果接入。
- `virtual_clock.py`：虚拟时间，用于长期状态更新。

### `RAANav/AgenticRAG/semantic_map_Update/`

语义地图更新模块。

重要文件：

- `map_Update.py`：地图更新主逻辑。
- `object_Update.py`：物体状态、位置和存在概率更新。
- `room_Update.py`：房间级信息更新。

### `RAANav/AgenticRAG/CLIP_RAG/`

CLIP 检索相关模块。

重要文件：

- `build_clip_index.py`：构建 CLIP 图像或物体索引。
- `query_clip_index.py`：查询 CLIP 索引。
- `runtime_clip_index.py`：运行时动态 CLIP 索引，用于在线观测物体的检索。

### `RAANav/AgenticRAG/GMM_map_Create/`

GMM 概率场与 Agent/RAG 融合评分相关模块。

重要文件：

- `GMM_map_calcualte.py`：根据候选目标构建 GMM 概率场。
- `agent_Rscore.py`：Agent 评分或 R-score 计算逻辑。

### `RAANav/AgenticRAG/scripts/`

导航、感知、前端和历史实验脚本目录。

重要子目录和文件：

- `scripts/z_legacy/legacy_gdino_frontend/sim_nav_loop_gdino.py`：真实 Habitat + 远程 GroundingDINO/MobileSAM/CLIP 的闭环 smoke test 脚本。
- `scripts/z_legacy/legacy_gdino_frontend/perception_gdino_mobilesam.py`：旧版本地 GroundingDINO + MobileSAM 感知流程。
- `scripts/nav_core/habitat_agent.py`：Habitat agent 封装。
- `scripts/nav_core/nav_strategy.py`：导航策略。
- `scripts/nav_core/perception.py`：导航核心感知抽象。
- `scripts/nav_core/voronoi_navigator.py`：Voronoi 导航器。
- `scripts/nav_core/visualization.py`：轨迹、地图、结果可视化。
- `scripts/raanav_frontend/`：RAANav 前端或实验界面相关脚本。
- `scripts/daaam/`：DAAAM 相关算法和实验代码。

### `RAANav/AgenticRAG/config/`

配置文件目录，保存地图构建、更新、导航、实验参数等配置。

### `RAANav/AgenticRAG/checkpoints/`

模型权重或 checkpoint 存放目录。大模型权重通常不建议直接提交到 Git，应通过文档说明下载或挂载路径。

### `RAANav/AgenticRAG/eval/`

算法实验和消融评测脚本。

重要文件：

- `gmm_evaluator.py`：GMM 概率场评测。
- `nav_sim_lightweight.py`：轻量导航仿真。
- `run_experiment.py`：实验运行入口。
- `ablation.py`：消融实验逻辑。

### `RAANav/AgenticRAG/RAG_Graph/`、`task_output/`、`output_*`

实验输出目录，通常包含导航轨迹、观测帧、局部 RAG、地图文件和诊断结果。它们一般是运行生成物，不是核心源代码。

## 5. 数据集与任务集关系

当前数据集内容主要分布如下：

- 原始静态场景：`hm3d/`
- 动态物体资产：`objects/`
- 物体图像目标：`objects_images/`
- 动态 layout 与中间生成结果：`results/`
- 数据集说明：`数据集基本信息.md`
- 任务集构建与评测：`benchmark/`

推荐理解顺序：

1. 先读 `数据集基本信息.md`。
2. 查看 `results/layouts/` 中动态 layout 的结构。
3. 查看 `benchmark/splits/` 和 `benchmark/episodes/` 中的任务划分与 episode。
4. 使用 `benchmark/evaluate.py` 或 `RAANav/AgenticRAG/benchmark_adapter/run_closed_loop.py` 运行评测。

## 6. 主要运行入口

### 长期闭环 benchmark

```bash
python -m RAANav.AgenticRAG.benchmark_adapter.run_closed_loop \
  --split-manifest benchmark/splits/benchmark_split_longterm_v1.json \
  --split val \
  --scene 00808-y9hTuugGdiq \
  --limit 1 \
  --output-dir benchmark/eval/raanav_closed_loop_smoke \
  --max-candidates 10 \
  --max-rounds 5
```

### 远程感知服务测试

```bash
python remote_vision_server/test_client.py \
  --base-url http://127.0.0.1:50220 \
  --image objects_images/Camera_01.webp \
  --text chair
```

### Habitat + 远程视觉 smoke test

```bash
python RAANav/AgenticRAG/scripts/z_legacy/legacy_gdino_frontend/sim_nav_loop_gdino.py \
  --scene-dir hm3d/minival/00808-y9hTuugGdiq \
  --dataset-config hm3d/hm3d_val_scene_dataset_config.json \
  --target mirror \
  --output-dir RAANav/AgenticRAG/RAG_Graph/remote_vision_nav_smoke \
  --max-steps 30 \
  --n-views 4 \
  --perception-backend remote \
  --remote-vision-base-url http://127.0.0.1:50220
```

### 导航结果诊断

```bash
python -m RAANav.AgenticRAG.benchmark_adapter.diagnose_nav_smoke \
  --output-dir RAANav/AgenticRAG/RAG_Graph/remote_vision_nav_smoke \
  --target mirror
```

## 7. 开发维护建议

- 数据集结构变动时，优先同步更新 `数据集基本信息.md` 和本文档。
- benchmark 字段变动时，优先检查 `benchmark/schemas.py`、`benchmark/build_episodes.py`、`benchmark/evaluate.py` 和 `RAANav/AgenticRAG/benchmark_adapter/`。
- 远程视觉模型接口变动时，优先同步修改 `remote_vision_server/server.py`、`remote_vision_server/client.py` 和 `remote_vision_server/README.md`。
- 长期能力实验应优先使用 `benchmark/` 与 `run_closed_loop.py`，`sim_nav_loop_gdino.py` 更适合作为真实 Habitat 感知闭环 smoke test。
- `benchmark/eval/`、`RAANav/AgenticRAG/RAG_Graph/`、`task_output/`、`output_*` 等目录多为实验输出，应避免将大体积结果误认为核心代码。
