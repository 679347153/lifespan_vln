# 工程结构说明

本文档说明当前工程的文件组织、主要模块职责与整体研究流程，便于后续维护、复现实验和继续完善“同一家庭环境下的长期具身能力”评测系统。

## 1. 项目总体定位

本项目面向动态家庭场景中的长期具身导航与开放词汇目标检索问题。其核心研究目标是：在同一物理家庭环境中，随着时间推移与物体布局变化，检验智能体是否能够积累、更新并利用跨 episode 的场景记忆，从而提升后续目标搜索、语言目标理解和图像目标定位能力。

从方法上看，本工程将动态布局数据集、Habitat-Sim 真实导航闭环、远程开放词汇视觉感知、CLIP 图文检索、Agent/RAG 融合评分和 benchmark 评测整合为一个可运行系统。系统并不只评估单次“是否找到目标”，而更关注智能体在同一家庭环境中随 `seen_layout_count_before` 增加后是否表现出长期经验积累效应。

## 2. 学术流程概述

项目流程可抽象为以下阶段：

1. **动态家庭状态构建**  
   基于 HM3D 静态家庭场景、3D 物体资产和物体图像，生成同一 scene 下多个时间状态的动态 layout。每个 layout 表示该家庭在某一时间点的物体摆放、房间关系和目标状态。

2. **长期任务集生成**  
   由动态 layout 构建 benchmark episode。每个 episode 包含 `scene_name`、`layout_id`、`state_index`、`seen_layout_count_before` 和若干 `subtasks`，用于描述同一家庭不同时间状态下的目标导航任务。

3. **真实 Habitat 闭环执行**  
   工作站加载 HM3D 场景和动态 layout，在 Habitat-Sim 中渲染 RGB-D 观测并执行基于 navmesh 的真实路径规划。系统使用 `--load-layout-objects` 将动态物体加入仿真环境，并使用 `--no-euclidean-fallback` 避免仅依赖欧氏距离产生失真的评测结果。

4. **远程开放词汇视觉感知**  
   GroundingDINO、MobileSAM 和 CLIP 在远程 GPU 服务器上运行。工作站通过 HTTP 或 SSH tunnel 调用远程服务，获得检测框、分割 mask、世界坐标投影所需信息和 CLIP image embedding。

5. **场景级长期记忆更新**  
   智能体将每次环视观测中的物体检测结果写入 scene-level memory，维护 object identity、位置、CLIP embedding、稳定性、存在概率、共现统计、负反馈等信息。该记忆跨同一 scene 的不同 layout 和 episode 累积。

6. **Agentic-RAG 候选检索与融合评分**  
   对当前目标查询，系统从长期记忆中检索候选，结合 label/alias、CLIP 文本-图像相似度、时间衰减、物体稳定性、存在概率、负反馈和 Agent 先验等因素进行排序。候选结果用于决定下一轮导航目标。

7. **闭环搜索与负反馈**  
   智能体移动到候选位置后再次观测。如果到达候选区域后仍未发现目标，则记录 negative feedback，降低旧候选权重，促使后续搜索从历史位置迁移到新的高概率区域。

8. **长期能力评测**  
   benchmark 统计 SR、SPL、found rate、dynamic memory accuracy、GHR@3/GHR@5、平均步数、平均最短距离和按 `seen_layout_count_before` 分组的长期曲线。理想结果是在同一 scene 下历史布局数量增加时，目标定位质量和导航成功率提升。

## 3. 顶层目录结构

### `hm3d/`

HM3D 场景数据目录，是 Habitat-Sim 仿真和 benchmark 评测的原始场景来源。

常见内容：

- `hm3d/minival/`：小规模验证场景。
- `hm3d/val/`：验证集场景。
- `hm3d/*_scene_dataset_config.json`：Habitat-Sim 加载场景所需的数据集配置。
- `hm3d/scene_info_export/`：部分场景信息导出结果。

### `objects/`

动态 layout 使用的 3D 物体资产目录。通常每个物体包含 `.gltf` 模型、纹理和相关配置，用于在同一 HM3D 家庭环境中生成不同时间状态下的物体摆放。

### `objects_images/`

物体图像目录，主要用于 image-goal 任务、VLM 物体语义理解、CLIP 图像索引、目标图像检索以及动态物体语义对齐。

### `results/`

动态布局生成和数据集中间结果目录。

常见子目录：

- `results/scene_info/`：场景级信息，包括房间、语义、结构化场景信息等。
- `results/probabilities/`：物体与房间、位置或承载面的概率关系。
- `results/receptacle_queries/`：承载面、可放置区域和房间内可交互对象查询结果。
- `results/object_instance_assignments/`：动态物体实例到房间或承载面的分配结果。
- `results/layouts/`：生成的动态布局，是 `layout_id` 和 `state_index` 的重要来源。
- `results/room_instances/`：房间实例、房间类型和相关结构化信息。

### `benchmark/`

长期任务集构建、划分、运行和评测目录。正式长期能力实验应以该目录和 `RAANav/AgenticRAG/benchmark_adapter/` 为主线。

重要文件：

- `benchmark/schemas.py`：episode、subtask、trajectory 等数据结构定义。
- `benchmark/build_episodes.py`：根据动态 layout 构建 benchmark episode。
- `benchmark/runner.py`：轻量 benchmark 运行入口。
- `benchmark/evaluate.py`：轨迹评测入口，计算任务成功和路径指标。
- `benchmark/metrics.py`：SR、SPL、探索曲线和分组指标计算。
- `benchmark/habitat_adapter.py`：Habitat-Sim 场景加载、navmesh 距离和路径规划适配层。

重要子目录：

- `benchmark/episodes/`：生成后的 episode JSON 文件。
- `benchmark/splits/`：训练、验证、测试划分文件，例如 `benchmark_split_longterm_v1.json`。
- `benchmark/eval/`：实验和评测输出目录。

### `RAANav/`

RAANav 与 Agentic-RAG 算法主体目录。

重要子目录：

- `RAANav/AgenticRAG/`：当前算法、闭环执行和 benchmark 适配主体。
- `RAANav/计划/`：阶段计划、执行报告、环境构建、命令汇总和问题记录。
- `RAANav/z_legacy/`：历史代码或旧版本实现。
- `RAANav/重要信息备份/`：重要中间信息和备份材料。

### `remote_vision_server/`

远程视觉模型服务目录。该模块将 GroundingDINO、MobileSAM 和 CLIP 放在服务器 GPU 上运行，工作站通过 HTTP 或 SSH tunnel 调用，从而避免在 Habitat 工作站上安装和加载重视觉模型。

重要文件：

- `server.py`：FastAPI 服务端，提供检测、分割、CLIP image embedding 和 CLIP text-image similarity 接口。
- `client.py`：工作站侧 Python 客户端，被真实闭环导航代码调用。
- `test_client.py`：远程服务连通性和功能测试脚本。
- `check_server_env.py`：服务器环境检查脚本。
- `run_server.sh`：服务器侧启动脚本。
- `workstation_tunnel.sh`：工作站侧 SSH tunnel 辅助脚本。
- `README.md`：远程视觉服务部署和调用说明。
- `REMOTE_CLIP_EMBEDDING.md`：远程 CLIP embedding 与文本-图像相似度说明。
- `GROUNDINGDINO_TORCH_API_FIX.md`：GroundingDINO 与 PyTorch API 兼容问题记录。
- `WORKSTATION_CLIP_FIX.md`：工作站侧 CLIP 离线化和远程化问题记录。

### `scripts/`

根目录下的数据集和 benchmark 辅助脚本。

重要文件：

- `01_prepare_splits.py`：准备 benchmark 数据划分。
- `03_generate_benchmark_episodes.py`：生成 benchmark episode。
- `04_validate_episodes.py`：校验 episode 合法性。
- `05_evaluate_benchmark.py`：运行 benchmark 评测。

### `__pycache__/`

Python 缓存目录，可再生成，不属于核心源代码。

## 4. 根目录重要文件

### `数据集基本信息.md`

数据集说明文档。它解释数据来源、目录结构、动态 layout、任务字段和评测目标，是理解 `hm3d/`、`objects/`、`objects_images/`、`results/` 和 `benchmark/` 的首要入口。

### `AgenticRAG_可执行技术规范_v1.md`

Agentic-RAG 方案的可执行技术规范，描述方法设计、模块职责、检索与决策流程、地图表示、融合评分等内容。

### `工程结构说明.md`

本文档，用于说明工程结构、主要流程和维护入口。

### `hm3d_paths.py`

HM3D 场景路径解析工具，用于在不同机器和不同目录结构下定位场景文件和 Habitat dataset config。

### `log_filter.py`

命令运行与日志过滤辅助脚本。常用于包装 Habitat、Hydra、OpenGL、模型加载等日志较多的命令，保留核心输出。

### `.gitignore`

Git 忽略规则，控制缓存、模型权重、生成结果等文件是否进入版本管理。

## 5. `RAANav/AgenticRAG/` 主体结构

### `benchmark_adapter/`

连接 benchmark episode、Agentic-RAG 记忆检索和真实 Habitat 闭环的核心适配层。

重要文件：

- `run_temporal_habitat_loop.py`：当前真实长期闭环主入口。它按 scene/layout 时间顺序运行 episode，调用远程视觉、更新 scene memory、生成候选、执行 Habitat 导航并调用 benchmark evaluate。
- `habitat_vision_loop.py`：真实 Habitat RGB-D 闭环封装，负责场景重置、环视观测、远程检测调用、深度投影和路径执行。
- `temporal_memory.py`：scene-level 长期记忆结构，维护物体轨迹、迁移、稳定性、存在概率和负反馈。
- `formal_scoring.py`：正式 Agent/RAG 融合评分逻辑，支持 memory、time decay、co-occurrence、negative feedback、CLIP 和 ablation 设置。
- `run_closed_loop.py`：离线闭环评测入口，适合调试 episode 对齐、记忆构建和候选评分，不依赖真实视觉。
- `dataset_index.py`：索引 split、scene、layout 和 episode 信息。
- `layout_to_floors.py`：将动态 layout 转换为 Agentic-RAG 可使用的 Floor/Room/Object 结构。
- `episode_to_queries.py`：将 benchmark subtask 转换为检索查询。
- `image_goal_index.py`：image-goal 图像索引与目标模态处理。
- `memory_builder.py`：从 layout 或历史信息构建初始记忆。
- `offline_eval.py`：离线评测与 episode 加载工具。
- `diagnose_temporal_run.py`：诊断真实长期闭环输出，汇总感知、候选、记忆、负反馈和失败归因。
- `diagnose_nav_smoke.py`：诊断 legacy 单目标导航输出。
- `lightweight_search.py`：轻量搜索工具，用于快速候选或路径实验。

### `semantic_map/`

语义地图基础数据结构。

重要文件：

- `floor.py`：楼层结构。
- `room.py`：房间结构。
- `object.py`：物体结构。
- `embedding_io.py`：embedding 读写工具。

### `semantic_map_Create/`

语义地图构建模块。

重要文件：

- `map_Create.py`：地图创建主流程。
- `scene_extract.py`：从场景中抽取结构化信息。
- `occupancy_grid.py`：从 navmesh 或深度观测构建占据栅格。
- `room_segmentation.py`：房间分割。
- `voronoi_graph.py`：Voronoi 图构建，用于导航候选和路径规划。
- `perception.py`：感知结果接入。
- `virtual_clock.py`：虚拟时间，用于长期状态更新。

### `semantic_map_Update/`

语义地图更新模块。

重要文件：

- `map_Update.py`：地图更新主逻辑。
- `object_Update.py`：物体状态、位置和存在概率更新。
- `room_Update.py`：房间级信息更新。

### `CLIP_RAG/`

CLIP 检索相关模块。

重要文件：

- `build_clip_index.py`：构建 CLIP 图像或物体索引。
- `query_clip_index.py`：查询 CLIP 索引。
- `runtime_clip_index.py`：运行时动态 CLIP 索引，用于在线观测物体检索。

### `GMM_map_Create/`

GMM 概率场与 Agent/RAG 融合评分相关模块。

重要文件：

- `GMM_map_calcualte.py`：根据候选目标构建 GMM 概率场。
- `agent_Rscore.py`：Agent 评分或 R-score 计算逻辑。

### `scripts/`

导航、感知、前端和历史实验脚本目录。

重要子目录和文件：

- `scripts/z_legacy/legacy_gdino_frontend/sim_nav_loop_gdino.py`：真实 Habitat + 远程 GroundingDINO/MobileSAM/CLIP 的单目标闭环 smoke test 脚本。
- `scripts/z_legacy/legacy_gdino_frontend/perception_gdino_mobilesam.py`：旧版本地 GroundingDINO + MobileSAM 感知流程。
- `scripts/nav_core/`：Habitat agent、导航策略、感知抽象、Voronoi 导航器和可视化。
- `scripts/raanav_frontend/`：RAANav 前端或实验界面相关脚本。
- `scripts/daaam/`：DAAAM 相关算法和实验代码。
- `scripts/visualmap/`、`scripts/ros1/`：历史可视化与 ROS 相关支持代码。

### `config/`

配置文件目录，保存地图构建、更新、导航和实验参数。

### `checkpoints/`

模型权重或 checkpoint 存放目录。大模型权重通常不应提交到 Git，应通过文档说明下载、挂载或软链接路径。

### `eval/`

算法实验和消融评测脚本。

重要文件：

- `gmm_evaluator.py`：GMM 概率场评测。
- `nav_sim_lightweight.py`：轻量导航仿真。
- `run_experiment.py`：实验运行入口。
- `ablation.py`：消融实验逻辑。

### 运行输出目录

以下目录通常是实验输出或历史输出，不应误认为核心源代码：

- `RAANav/AgenticRAG/RAG_Graph/`
- `RAANav/AgenticRAG/task_output/`
- `RAANav/AgenticRAG/output_*`
- `RAANav/AgenticRAG/deep_explore_v3/`
- `benchmark/eval/`

## 6. 数据集、任务与系统模块关系

当前长期能力评测的数据流如下：

1. `hm3d/` 提供静态家庭场景和 Habitat dataset config。
2. `objects/` 提供可插入动态 layout 的 3D 物体资产。
3. `objects_images/` 提供 image-goal 和 CLIP/VLM 对齐图像。
4. `results/layouts/` 提供同一 scene 下多个时间状态的动态 layout。
5. `benchmark/build_episodes.py` 将 layout 与任务定义转换为 episode。
6. `benchmark/splits/` 定义 train/val/test 或 longterm split。
7. `run_temporal_habitat_loop.py` 按 episode 执行真实闭环。
8. `remote_vision_server/` 提供远程开放词汇视觉能力。
9. `temporal_memory.py` 保存和更新 scene-level 长期记忆。
10. `benchmark/evaluate.py` 对 trajectory 和 subtask trace 进行评测。

推荐理解顺序：

1. 阅读 `数据集基本信息.md`，理解数据字段和长期评测目标。
2. 查看 `results/layouts/`，理解动态 layout 的结构。
3. 查看 `benchmark/splits/` 与 `benchmark/episodes/`，理解任务划分和 episode 表达。
4. 阅读 `RAANav/AgenticRAG/benchmark_adapter/run_temporal_habitat_loop.py`，理解真实闭环执行逻辑。
5. 阅读 `remote_vision_server/README.md`，理解服务器端视觉模型部署和工作站调用方式。
6. 使用 `RAANav/计划/推荐运行命令汇总_2026-06-22.md` 中的命令进行 smoke test、单 scene 时间序列实验和 val split 实验。

## 7. 主要运行入口

### 真实长期 Habitat 闭环 smoke test

这是当前主线的最小真实闭环验证命令。它只跑一个 scene、一个 layout、一个 episode，用于确认 Habitat、远程视觉、动态 layout、scene memory 和 benchmark evaluate 主链路可以跑通。

```bash
python -m RAANav.AgenticRAG.benchmark_adapter.run_temporal_habitat_loop \
  --split-manifest benchmark/splits/benchmark_split_longterm_v1.json \
  --split val \
  --scene 00808-y9hTuugGdiq \
  --layout-limit 1 \
  --episodes-per-layout 1 \
  --output-dir benchmark/eval/longterm_habitat_loop_smoke \
  --max-steps-per-subtask 20 \
  --max-rounds 2 \
  --micro-steps-per-round 4 \
  --n-views 4 \
  --perception-backend remote \
  --remote-vision-base-url http://127.0.0.1:50220 \
  --load-layout-objects \
  --no-euclidean-fallback
```

### 单 scene 时间序列实验

用于检验同一 scene 下 memory 是否跨 layout 累积。

```bash
python -m RAANav.AgenticRAG.benchmark_adapter.run_temporal_habitat_loop \
  --split-manifest benchmark/splits/benchmark_split_longterm_v1.json \
  --split val \
  --scene 00808-y9hTuugGdiq \
  --episodes-per-layout 1 \
  --output-dir benchmark/eval/longterm_habitat_loop_00808_one_ep_per_layout \
  --max-steps-per-subtask 40 \
  --max-rounds 3 \
  --micro-steps-per-round 6 \
  --n-views 4 \
  --perception-backend remote \
  --remote-vision-base-url http://127.0.0.1:50220 \
  --load-layout-objects \
  --no-euclidean-fallback
```

### 离线闭环调试

不依赖真实视觉服务，适合快速检查 episode、layout、候选评分和 benchmark evaluate 对齐。

```bash
python -m RAANav.AgenticRAG.benchmark_adapter.run_closed_loop \
  --split-manifest benchmark/splits/benchmark_split_longterm_v1.json \
  --split val \
  --scene 00808-y9hTuugGdiq \
  --limit 1 \
  --output-dir benchmark/eval/raanav_closed_loop_smoke \
  --max-candidates 10 \
  --max-rounds 5
```

### 远程视觉服务测试

```bash
python remote_vision_server/test_client.py \
  --base-url http://127.0.0.1:50220 \
  --image objects_images/Camera_01.webp \
  --text chair
```

### 真实长期闭环结果诊断

```bash
python -m RAANav.AgenticRAG.benchmark_adapter.diagnose_temporal_run \
  --output-dir benchmark/eval/longterm_habitat_loop_smoke \
  --write-json benchmark/eval/longterm_habitat_loop_smoke/temporal_run_diagnosis.json
```

### Legacy 单目标导航 smoke test

旧脚本不按 benchmark episode 时间序列运行，但适合检查单目标真实感知闭环。

```bash
python RAANav/AgenticRAG/scripts/z_legacy/legacy_gdino_frontend/sim_nav_loop_gdino.py \
  --scene-dir hm3d/minival/00808-y9hTuugGdiq \
  --dataset-config hm3d/hm3d_val_scene_dataset_config.json \
  --target mirror \
  --output-dir RAANav/AgenticRAG/RAG_Graph/remote_vision_nav_smoke \
  --max-steps 30 \
  --n-views 4 \
  --perception-backend remote \
  --remote-vision-base-url http://127.0.0.1:50220
```

## 8. 输出与诊断文件

真实长期闭环运行后，重点关注以下输出：

- `run.jsonl`：每个 episode 的轨迹和 subtask trace。
- `summary.json`：benchmark 总指标，包括 SR、SPL、平均路径长度和动态记忆准确率。
- `temporal_summary.json`：按 `state_index` 和 `seen_layout_count_before` 聚合的长期指标。
- `perception_summary.json`：远程视觉调用次数、失败次数、检测数量和平均耗时。
- `candidate_traces.jsonl`：每轮候选、选中候选、fallback 原因、规划点和移动结果。
- `observation_traces.jsonl`：每次环视检测到的 label、bbox、位置和 CLIP embedding 维度。
- `memory_updates.jsonl`：scene memory 的新建、合并、迁移和负反馈事件。
- `negative_feedback.jsonl`：到达候选后未发现目标时的负反馈记录。
- `layout_transitions.jsonl`：进入和退出 layout 时的 memory snapshot 信息。
- `scene_memory/` 与 `memory_snapshots/`：长期记忆最终状态和阶段性快照。

当 `success_rate=0.0` 时，不应立即判定算法失败。应结合 `candidate_traces.jsonl`、`observation_traces.jsonl`、`memory_updates.jsonl` 和 `diagnose_temporal_run.py` 的输出区分以下情况：

- 感知为空或检测不足。
- memory 没有更新。
- 候选检索为空。
- 候选存在但距离真实目标过远。
- 候选接近目标但导航预算或成功半径导致失败。
- 旧位置负反馈不足，动态目标迁移能力不充分。

## 9. 开发维护建议

- 数据集结构变动时，优先同步更新 `数据集基本信息.md` 和本文档。
- benchmark 字段变动时，优先检查 `benchmark/schemas.py`、`benchmark/build_episodes.py`、`benchmark/evaluate.py` 和 `RAANav/AgenticRAG/benchmark_adapter/`。
- 真实长期闭环变动时，优先检查 `run_temporal_habitat_loop.py`、`habitat_vision_loop.py`、`temporal_memory.py` 和 `formal_scoring.py`。
- 远程视觉模型接口变动时，优先同步修改 `remote_vision_server/server.py`、`remote_vision_server/client.py`、`remote_vision_server/test_client.py` 和 `remote_vision_server/README.md`。
- 正式长期能力实验应优先使用 `run_temporal_habitat_loop.py`；`run_closed_loop.py` 用于离线调试，`sim_nav_loop_gdino.py` 用于 legacy 单目标 smoke test。
- `benchmark/eval/`、`RAANav/AgenticRAG/RAG_Graph/`、`task_output/`、`output_*` 等目录多为实验输出，应避免将大体积结果误认为核心代码。
- 模型权重、Habitat 大数据和生成结果应通过路径配置、下载文档或服务器挂载管理，不建议直接纳入 Git。
