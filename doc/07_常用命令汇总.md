# 常用命令汇总

本文档集中保存常用命令，便于复制执行。

## 1. 服务器端启动远程视觉服务

```bash
export REMOTE_VISION_REPO_ROOT=$PWD
export REMOTE_VISION_DEVICE=cuda
export REMOTE_VISION_HOST=127.0.0.1
export REMOTE_VISION_PORT=8010
export REMOTE_VISION_EAGER_LOAD=1
export REMOTE_VISION_RETURN_CLIP=1
export REMOTE_VISION_CLIP_MODEL_PATH=/home/ma-user/work/zhangWei/mtu3d/data/trans/clip-vit-large-patch14
export REMOTE_VISION_CLIP_LOCAL_FILES_ONLY=1

python log_filter.py --run "bash remote_vision_server/run_server.sh"
```

## 2. 工作站建立 SSH tunnel

```bash
export SSHPASS='<server-password>'
export REMOTE_VISION_SSH_HOST=7.216.187.6
export REMOTE_VISION_SSH_PORT=30180
export REMOTE_VISION_SSH_USER=root
export REMOTE_VISION_LOCAL_PORT=50220
export REMOTE_VISION_REMOTE_PORT=8010

python log_filter.py --run "bash remote_vision_server/workstation_tunnel.sh"
```

## 3. 检查远程服务

```bash
python log_filter.py --run "curl http://127.0.0.1:50220/health"
python log_filter.py --run "curl http://127.0.0.1:50220/v1/models"
```

## 4. 远程视觉单图测试

```bash
python log_filter.py --run "python remote_vision_server/test_client.py \
  --base-url http://127.0.0.1:50220 \
  --image objects_images/Camera_01.webp \
  --prompt 'camera . chair . table .' \
  --return-clip"
```

## 5. 单 scene / 单 layout / 单 episode smoke

```bash
python log_filter.py --run "python -m RAANav.AgenticRAG.benchmark_adapter.run_temporal_habitat_loop \
  --split-manifest benchmark/splits/benchmark_split_longterm_v1.json \
  --split val \
  --scene 00808-y9hTuugGdiq \
  --layout-limit 1 \
  --episodes-per-layout 1 \
  --output-dir benchmark/eval/longterm_habitat_loop_smoke \
  --planning-mode room \
  --max-steps-per-subtask 40 \
  --max-rounds 3 \
  --micro-steps-per-round 6 \
  --n-views 4 \
  --clip-min-score 0.15 \
  --target-name-map RAANav/AgenticRAG/config/target_name_map_longterm_v1.json \
  --normalize-target-names \
  --target-confirmation-mode spatial \
  --target-confirmation-margin 1.0 \
  --perception-backend remote \
  --remote-vision-base-url http://127.0.0.1:50220 \
  --load-layout-objects \
  --no-euclidean-fallback"
```

## 6. 单 scene 全 layout 验证

```bash
python log_filter.py --run "python -m RAANav.AgenticRAG.benchmark_adapter.run_temporal_habitat_loop \
  --split-manifest benchmark/splits/benchmark_split_longterm_v1.json \
  --split val \
  --scene 00808-y9hTuugGdiq \
  --episodes-per-layout 1 \
  --output-dir benchmark/eval/longterm_00808_one_ep_per_layout \
  --planning-mode room \
  --max-steps-per-subtask 80 \
  --max-rounds 5 \
  --micro-steps-per-round 8 \
  --n-views 4 \
  --clip-min-score 0.15 \
  --target-name-map RAANav/AgenticRAG/config/target_name_map_longterm_v1.json \
  --normalize-target-names \
  --target-confirmation-mode spatial \
  --target-confirmation-margin 1.0 \
  --perception-backend remote \
  --remote-vision-base-url http://127.0.0.1:50220 \
  --load-layout-objects \
  --no-euclidean-fallback"
```

## 7. Object planning 消融

```bash
python log_filter.py --run "python -m RAANav.AgenticRAG.benchmark_adapter.run_temporal_habitat_loop \
  --split-manifest benchmark/splits/benchmark_split_longterm_v1.json \
  --split val \
  --scene 00808-y9hTuugGdiq \
  --layout-limit 1 \
  --episodes-per-layout 1 \
  --output-dir benchmark/eval/object_planning_ablation \
  --planning-mode object \
  --max-steps-per-subtask 80 \
  --max-rounds 5 \
  --micro-steps-per-round 8 \
  --n-views 4 \
  --perception-backend remote \
  --remote-vision-base-url http://127.0.0.1:50220 \
  --load-layout-objects \
  --no-euclidean-fallback"
```

## 8. 诊断输出

```bash
python log_filter.py --run "python -m RAANav.AgenticRAG.benchmark_adapter.diagnose_temporal_run \
  --output-dir benchmark/eval/longterm_habitat_loop_smoke \
  --write-json benchmark/eval/longterm_habitat_loop_smoke/diagnosis.json"
```

## 9. HTML 可视化

```bash
python log_filter.py --run "python -m RAANav.AgenticRAG.benchmark_adapter.visualize_temporal_run \
  --output-dir benchmark/eval/longterm_habitat_loop_smoke"
```

## 10. 生成 subtask 级搜索视频

```bash
python log_filter.py --run "python -m RAANav.AgenticRAG.benchmark_adapter.run_temporal_habitat_loop \
  --split-manifest benchmark/splits/benchmark_split_longterm_v1.json \
  --split val \
  --scene 00808-y9hTuugGdiq \
  --layout-limit 1 \
  --episodes-per-layout 1 \
  --output-dir benchmark/eval/video_smoke \
  --planning-mode room \
  --max-steps-per-subtask 40 \
  --max-rounds 3 \
  --micro-steps-per-round 6 \
  --n-views 4 \
  --record-search-video \
  --video-subtasks '0:0' \
  --video-output-dir benchmark/eval/video_smoke/videos \
  --video-include-observation-views \
  --video-width 1920 \
  --video-height 1080 \
  --perception-backend remote \
  --remote-vision-base-url http://127.0.0.1:50220 \
  --load-layout-objects \
  --no-euclidean-fallback"
```

## 11. 过滤 Habitat 日志运行

```bash
python log_filter.py --run "python -m RAANav.AgenticRAG.benchmark_adapter.run_temporal_habitat_loop \
  --split-manifest benchmark/splits/benchmark_split_longterm_v1.json \
  --split val \
  --scene 00808-y9hTuugGdiq \
  --layout-limit 1 \
  --episodes-per-layout 1 \
  --output-dir benchmark/eval/log_filter_smoke \
  --perception-backend remote \
  --remote-vision-base-url http://127.0.0.1:50220 \
  --load-layout-objects \
  --no-euclidean-fallback"
```

## 12. 静态检查

```bash
python log_filter.py --run "python -m py_compile \
  RAANav/AgenticRAG/benchmark_adapter/run_temporal_habitat_loop.py \
  RAANav/AgenticRAG/benchmark_adapter/habitat_vision_loop.py \
  RAANav/AgenticRAG/benchmark_adapter/temporal_memory.py \
  RAANav/AgenticRAG/benchmark_adapter/visualize_temporal_run.py \
  RAANav/AgenticRAG/benchmark_adapter/diagnose_temporal_run.py \
  RAANav/AgenticRAG/benchmark_adapter/search_video.py"
```

## 13. 查看帮助

```bash
python log_filter.py --run "python -m RAANav.AgenticRAG.benchmark_adapter.run_temporal_habitat_loop --help"
python log_filter.py --run "python -m RAANav.AgenticRAG.benchmark_adapter.diagnose_temporal_run --help"
python log_filter.py --run "python -m RAANav.AgenticRAG.benchmark_adapter.visualize_temporal_run --help"
```
